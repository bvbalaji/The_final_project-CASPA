[build]
  command = "pip install -r requirements.txt"
  publish = "dist"  # The folder containing your final HTML files
  functions = "netlify/functions"
  
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
